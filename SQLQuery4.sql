SELECT * FROM person.Person; -- Seleciona todos os dados da tabela person.Person.

SELECT firstName, LastName FROM person.Person; -- Seleciona somente dados da coluna firstName e LastName da tabela person.Person.

SELECT DISTINCT firstName, LastName FROM person.Person; -- DISTINCT é usado para NÂO EXIBIR dados duplicados. 
